# Shared Docker network used by Nginx and the backend.
DISH_PATCH_NETWORK_NAME=dishpatch-network

# Safe localhost-only port for testing Docker Nginx.
NGINX_TEST_BIND_IP=127.0.0.1
NGINX_TEST_HTTP_PORT=18080